# Dependencies
node_modules/

# Firebase
.firebase/
firebase-debug.log
*-debug.log

# OS / editor
.DS_Store
Thumbs.db

# Large intermediate data dumps (not loaded by the app)
data/output.json
data/bike_data_organized.json

# Local-only working docs & tooling (not for the repo)
docs/superpowers/
PROJECT_INDEX.md
.superpowers/
.claude/
docs/firebase_token.md

# Python
__pycache__/

# Raw data inputs / one-off script outputs (only data/accidents.json and
# data/bike_data.json are loaded by the site)
data.csv
bike_stop.csv
headers.txt
fatal_count.txt
*.pdf
*.kml

# build_road_stats.py: raw Overpass API response cache. Can be tens of MB and
# is fully reproducible by re-running the script (it re-downloads when this
# file is absent); data/road_stats.json (the derived, committed output) is
# what the app/map feature actually reads.
data/osm_roads_cache.json

